// Definindo a classe Trip
class Trip {
    // Implementando o construtor da classe
    constructor(
      id,
      usuario,
      ano,
      estado
    ) {
      this.id = id
      this.usuario = usuario
      this.ano = ano
      this.estado = estado
    }
  }
  
  module.exports = Trip
  